# L2 HLD UseCase6 deliverable 8.2 "Frontend Interface." Copy to `.env` and
# point at a running banking-support-service instance (see
# ../banking-support-service/README section or ../README.md's reproduction
# steps). Defaults to the mock server in mock-server/ for local frontend
# development when no real backend is running -- see mock-server/README.md.
VITE_API_BASE_URL=http://localhost:8085
