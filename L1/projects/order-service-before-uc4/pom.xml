<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.4</version>
    <relativePath/>
  </parent>

  <groupId>com.retailco</groupId>
  <artifactId>order-service</artifactId>
  <version>0.9.0-pre-review</version>
  <name>order-service (BEFORE - pre code-review)</name>
  <description>
    L1 UC4 input artifact: intentionally contains the defect classes called out
    in the use case brief (missing null checks, high complexity, hardcoded
    secrets, a hallucinated API, license risk) so the AI-review vs SonarQube
    comparison has real findings to compare. DO NOT deploy this version.
  </description>

  <properties>
    <java.version>17</java.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- LICENSE RISK (flagged in reviews/): iText 5 core is licensed under
         AGPL-3.0 (copyleft) unless a commercial license is purchased. Pulling
         it in for a single PDF-receipt helper is a licensing liability for a
         proprietary product and was never approved by legal. See
         reviews/sonarqube-style-report.md and reviews/ai-review-report.json. -->
    <dependency>
      <groupId>com.itextpdf</groupId>
      <artifactId>itextpdf</artifactId>
      <version>5.5.13.3</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

</project>
