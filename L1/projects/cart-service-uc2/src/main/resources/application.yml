server:
  port: 8082

spring:
  application:
    name: cart-service

services:
  product-catalog:
    base-url: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health,info
